{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["**/*"],
        "buildCommand": "cd client && yarn install && yarn build"
      }
    }
  ]
}
